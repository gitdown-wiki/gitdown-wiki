{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <ul>
                {% for wiki in wikis %}
                <li><a href="{{ url('wiki_showpage', {'slug': wiki.slug}) }}">{{ wiki.name }}</a></li>
                
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
