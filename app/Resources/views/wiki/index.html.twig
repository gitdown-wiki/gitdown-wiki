{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <ul>
                {% for wiki in wikis %}
                <li><a href="{{ url('page_show', {'slug': wiki.slug}) }}">{{ wiki.name }}</a></li>
                
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
